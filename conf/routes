# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

POST	/searchTicket				controllers.Application.searchTicket()

POST    /createTicket               controllers.Application.createTicket()

POST    /saveTicket					controllers.Application.saveTicket()

POST	/addComment					controllers.Application.addComment()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
